#!/bin/bash
set -Eeuo pipefail

for file in sass/*; do
    name="$(basename -- "$file")"
    if [[ $name =~ ^[^_] ]]; then
        newname="html/css/$(echo $name | sed -e 's/scss/css/' -)"
        echo "Rendering $file -> $newname"
        sass $file > $newname
    fi
done

